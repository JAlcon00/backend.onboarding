# Configuración de ejemplo para las mejoras de media prioridad
# Copiar a .env y ajustar valores según el entorno

# === CONFIGURACIÓN DE POOL DE BASE DE DATOS ===
# Conexiones mínimas en el pool (recomendado: 2-5 para desarrollo, 5-10 para producción)
DB_POOL_MIN=2

# Conexiones máximas en el pool 
# Desarrollo: 10-20, Producción: 20-50 (depende de la carga)
DB_POOL_MAX=10

# Tiempo máximo en ms para obtener una conexión (30 segundos)
DB_POOL_ACQUIRE=30000

# Tiempo en ms antes de liberar una conexión inactiva (10 segundos)
DB_POOL_IDLE=10000

# === CONFIGURACIÓN DE LOGS ===
# Habilitar logging de Winston en formato JSON estructurado
WINSTON_JSON_LOGS=true

# Nivel de log (error, warn, info, http, debug)
LOG_LEVEL=info

# Directorio para archivos de log rotatorios
LOG_DIR=./logs

# Retención de archivos de log (días)
LOG_RETENTION_DAYS=30

# Tamaño máximo por archivo de log antes de rotar
LOG_MAX_SIZE=20m

# === CONFIGURACIÓN DE AUDITORÍA ===
# Habilitar auditoría automática (recomendado: true en producción)
AUDIT_ENABLED=true

# Retención de registros de auditoría (días)
AUDIT_RETENTION_DAYS=2555  # 7 años para compliance

# Habilitar auditoría de operaciones de lectura (puede generar muchos logs)
AUDIT_READ_OPERATIONS=false

# === CONFIGURACIÓN DE CORRELACIÓN DE LOGS ===
# Prefijo para x-request-id generados automáticamente
REQUEST_ID_PREFIX=req

# Header personalizado para request ID (si no se usa x-request-id)
CUSTOM_REQUEST_ID_HEADER=x-correlation-id

# === CONFIGURACIÓN POR ENTORNO ===

# DESARROLLO
# DB_POOL_MIN=2
# DB_POOL_MAX=10
# LOG_LEVEL=debug
# AUDIT_READ_OPERATIONS=true

# PRODUCCIÓN
# DB_POOL_MIN=10
# DB_POOL_MAX=50
# LOG_LEVEL=info
# AUDIT_READ_OPERATIONS=false
# WINSTON_JSON_LOGS=true

# TESTING
# DB_POOL_MIN=1
# DB_POOL_MAX=5
# LOG_LEVEL=error
# AUDIT_ENABLED=false
